export function UploadPage() {
  return (
    <main className="page-shell">
      <section className="intro-section" aria-labelledby="upload-title">
        <p className="eyebrow">FilesXchange</p>
        <h1 id="upload-title">Share files by temporary link</h1>
        <p className="lead">
          Select files, upload them, and send one download link. Upload flow
          controls will be added in the next frontend stages.
        </p>
      </section>
    </main>
  )
}
